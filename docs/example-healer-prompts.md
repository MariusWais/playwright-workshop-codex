# Healer Prompts

@healer
Fix failing tests in tests/examples/policy-crud.spec.ts and re-run them. Summarize locator changes.

@healer
If tests fail due to timing, add appropriate waits rather than timeouts and retry.
